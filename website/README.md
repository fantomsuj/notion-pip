# Perch website

Marketing site for [pinapage.com](https://pinapage.com).

## Intro motion

On first paint the bird mark fades in, holds, then fades out before the page
copy fades in. Timings live in `lib/intro-motion.ts` and `app/globals.css`.

```sh
npm install
npm test
npm run dev
```
